[package]
name = "flux-telepathy"
version = "0.1.0"
edition = "2021"
description = "Agent-to-agent messaging with trust-weighted priority — the nervous system of the fleet"

[dependencies]
rand = "0.8"
